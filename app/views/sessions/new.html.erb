<div class="container my-5">
  <h1 class="text-center mb-4">Iniciar Sesión</h1>
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card shadow">
        <div class="card-body">
          <%= form_with url: login_path, local: true do |form| %>
            <div class="mb-3">
              <%= form.label :email, "Email", class: "form-label" %>
              <%= form.email_field :email, class: "form-control", placeholder: "Ingresa tu email" %>
            </div>
            <div class="mb-3">
              <%= form.label :password, "Contraseña", class: "form-label" %>
              <%= form.password_field :password, class: "form-control", placeholder: "Ingresa tu contraseña" %>
            </div>
            <div class="text-center">
              <%= form.submit "Iniciar Sesión", class: "btn btn-dark w-100" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
